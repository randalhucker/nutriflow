# Installation

This page explains how to install and run NutriFlow. By the time we have any real users, the app will be hosted on the web and available on the app store, so this entire document will become OBE.

> NutriFlow is under active development. Installation steps may change frequently.

## Requirements
- **TBD**: Node.js version
- **TBD**: Package manager (npm/yarn/pnpm)
- **TBD**: Database (if required)
- **TBD**: Backend service (if separate)

## Setup (Developer Build)
1. Clone the repository:
   - `git clone ...`
   - `cd nutriflow`

2. Install dependencies:
   - `npm i`

3. Configure environment variables:
   - Create a .env file and create the variables below

### Environment Variables
| Variable | Description | Example |
|---|---|---|
| `TBD` | API base URL | `http://localhost:3000` |
| `TBD` | Database URL | `postgres://...` |

## Run
- Start the app: `npm start`
- Start backend: `TBD`

## Common Installation Issues
See: [Troubleshooting](../help/troubleshooting.md)
